# Examples

This directory contains standalone scripts demonstrating GeoFusion AI capabilities.

## quick_start.py

Minimal example that generates synthetic shapes, trains a PointNet++ encoder for 5 epochs, and runs similarity search. Runs in under 60 seconds on CPU.

## Usage

```bash
python examples/quick_start.py
```
